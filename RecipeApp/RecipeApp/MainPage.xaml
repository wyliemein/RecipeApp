<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:d="http://xamarin.com/schemas/2014/forms/design" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="RecipeApp.MainPage">
    <StackLayout Spacing="30" Padding="30"
                 VerticalOptions="Center">
        <!-- Place new controls here -->
         <Label Text="Recipe App"
               HorizontalOptions="Center"
               FontAttributes="Bold" 
               TextColor="Green"/>
        <Entry Placeholder="Username" Text="{Binding Username}" />
        <Entry Placeholder="Password" Text="{Binding Password}" 
               IsPassword="true"/>
        <Button Text="Login" TextColor="Green"
                BackgroundColor="Gray"
                Command="{Binding LoginCommands}" Clicked="Login_OnClicked" />
        <Button Text="Dont Have an Account? Sign Up Here!" TextColor="Green"
                Command="{Binding SignUpCommands}" Clicked="SignUp_OnClicked"/>
    </StackLayout>
</ContentPage>